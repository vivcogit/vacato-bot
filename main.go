package main

func main() {
	Chatbot()
	// img, err := LoadImage("input.png")
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// DrawTextToImage(img, "Hello, World!\nMultiline text")

	// outFile, err := os.Create("output.png")
	// if err != nil {
	// 	log.Fatalf("Error creating output file: %v", err)
	// }
	// defer outFile.Close()

	// err = png.Encode(outFile, img)
	// if err != nil {
	// 	log.Fatalf("Error encoding output image: %v", err)
	// }

	// log.Println("Text added to the image successfully!")
}
